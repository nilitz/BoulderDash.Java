package entity;
/**
 * The class Player_One
 * @author HugoDegrossi
 */
public class Player_One extends Object {
    /**
     * Constructor
     * @param name
     * @param status
     * @param lastMove
     */
    public Player_One(String name, boolean status, LastMove lastMove) {
        super(name, status, lastMove);
    }
}
